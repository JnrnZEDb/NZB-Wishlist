define([
    "jquery",
    "underscore"
], function($, _) {

    /**
     * DO NOT MODIFY - Generated <%= Time.now.to_s %>
     * This file is generated by:
     * `bundle exec rake wishlist:js:routes`
     **/

    var my = {};

    var generate_route = function(tmpl, params) {
        if(params !== null && params !== undefined && params.format !== undefined) {
            tmpl += ".<%%= format %>";
        }
        return _.template(tmpl)(params || {});
    };
<% data.each do |name, route| %>
    my.<%= name %> = function(params) {
        return generate_route("<%= route %>", params);
    };
<% end %>
    return my;
});